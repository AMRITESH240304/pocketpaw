"""PocketPaw Enterprise Cloud — MongoDB-backed cloud features.

Replaces the NestJS paw-cloud service. Provides user auth, agent config,
pockets, sessions, comments, notifications, and file storage via FastAPI
routers backed by Beanie (async MongoDB ODM).
"""
